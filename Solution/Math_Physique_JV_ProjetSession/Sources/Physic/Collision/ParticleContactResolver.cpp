

void ParticleContactResolver::resolveContacts(ParticleContact* contactArray, unsigned int numContact, float duraction) {

	unsigned i;

	interactionUsed = 0;

	while(iteractionUsed < interaction) {
		
		for ( i = 0 ; if< numContact, i++) {
			double separatingVelocity = contactArray[i].calculateSeperatingVelocity();

			if (separatingVelocity)
		}
	}

}